myFunction();

function myFunction() {
  console.log("hello");
}

x = 10;
console.log(x);
var x;

//it means that the function is hoisted to the top of the scope
//declarations are moved to the top of their respectives scopes during the compilation phase
